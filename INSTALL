Thanks for downloading LsHw Windows Emulator.

To install it, make sure you have these programs installed:

    * Python >= 3.6
    * pip

To install from source:

    $ pip install .

To create a distribution package:

    $ pip install build
    $ cd bin
    $ ./create-package.bat
    # Packages will be created in dist/ directory

And enjoy!
